## What does this change?

<!-- What can a user do now that they could not before? -->

## Related issue

<!-- Fixes #123 -->

## Checklist

- [ ] `python3 -m unittest discover -s tests -q` passes
- [ ] New or rebuilt GIFs are under 180 KB (skip if you did not touch `gifs/` or `stills/`)
- [ ] No secrets, tokens, or `.env`
- [ ] Title is still read from an environment variable (not a shell interpolation)
